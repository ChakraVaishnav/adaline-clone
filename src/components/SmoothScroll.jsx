'use client';

/**
 * SmoothScroll Component - Now using native browser scroll
 * Lenis removed for better performance and responsiveness
 */
export default function SmoothScroll({ children }) {
    return <>{children}</>;
}
